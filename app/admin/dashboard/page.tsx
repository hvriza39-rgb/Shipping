import AdminDashboard from "@/components/admin/AdminDashboard";

export const metadata = {
  title: "Admin — Operations | SwiftShip",
};

export default function AdminDashboardPage() {
  return <AdminDashboard />;
}
